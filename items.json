{
  "items": [
    {
      "name": "골드 시세(1천만골)",
      "apiName": "골드",
      "scale": 10
    },
    {
      "name": "강화의 룬",
      "apiName": "포장된 강화의 룬",
      "scale": 1
    },
    {
      "name": "추출의 룬",
      "apiName": "포장된 추출의 룬",
      "scale": 1
    },
    {
      "name": "귀속 해제 포션",
      "apiName": "포장된 귀속 해제 포션",
      "scale": 1
    },
    {
      "name": "슬링샷 세트",
      "apiName": "포장된 스페셜 더블 볼륨업P+ 언리미티드 슬링샷 비키니 세트",
      "scale": 1
    },
    {
      "name": "격노 인챈트",
      "apiName": "인챈트 스크롤 (무기한)",
      "match": "격노",
      "scale": 1
    },
    {
      "name": "찬미 인챈트",
      "apiName": "인챈트 스크롤 (무기한)",
      "match": "찬미",
      "scale": 1
    },
    {
      "name": "일격 인챈트",
      "apiName": "인챈트 스크롤 (무기한)",
      "match": "일격",
      "scale": 1
    }
  ],
  "notices": []
}```

---

## 파일 3 — 파일명: `.github/workflows/collect.yml`
(파일명 칸에 경로까지 그대로 입력하면 폴더가 자동 생성됩니다)

```yaml
# 망전 시세 자동 수집 — 매일 KST 오전 9시·오후 6시 (크론은 UTC 기준)
# 수동 실행: Actions 탭 → collect-prices → Run workflow
name: collect-prices
on:
  schedule:
    - cron: '0 0 * * *'    # UTC 00:00 = KST 09:00
    - cron: '0 9 * * *'    # UTC 09:00 = KST 18:00
  workflow_dispatch:
permissions:
  contents: write
jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: 시세 수집
        run: node collect.mjs
        env:
          NEXON_API_KEY: ${{ secrets.NEXON_API_KEY }}
      - name: 커밋·푸시
        run: |
          git config user.name "price-bot"
          git config user.email "bot@users.noreply.github.com"
          git add data/prices.json
          git diff --cached --quiet || git commit -m "collect $(date -u +%F_%H%M)"
          git push
```
